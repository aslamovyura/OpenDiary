@model WebUI.ViewModels.Profile.ProfileViewModel
@{
    ViewBag.Title = Localizer["Title"];
}
    <div class="form-group row">
        <div class="col">
            <img class="mb-4" src="~/User.png" alt="" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" width="250" height="250" />
        </div>
        <div class="col">
            <div class="text-left">
                <h2 class="display-4">@Model.FirstName @Model.LastName</h2>
            </div>

            <div>
                <p>@Localizer["Email"] : @Model.Email</p>
                <p>@Localizer["BitrhDate"] : @Model.BirthDate (@Model.Age years)</p>
                <p>@Localizer["Posts"] : @Model.TotalPostsNumber</p>
                <p>@Localizer["Cоmments"] : @Model.TotalCommentsNumber</p>
            </div>
        </div>
    </div>

    <table class="table">
        <caption>@Localizer["PostsList"]</caption>
        <tr><th>@Localizer["Date"]</th><th>@Localizer["Posts"]</th><th></th></tr>
        @foreach (var post in Model.Posts)
        {
            <tr>
                <td>@post.Date</td>
                <td>@post.Title</td>
                <td>
                    <a class="btn btn-sm btn-primary" asp-action="Read" asp-controller="Posts" asp-route-postId="@post.Id">Read</a>
                </td>
            </tr>
        }
    </table>